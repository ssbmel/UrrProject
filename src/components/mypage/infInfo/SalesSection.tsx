import React from "react";
import SalesList from "./SalesList";

const SalesSection = () => {
  return (
    <section>
      <SalesList />
      <SalesList />
    </section>
  );
};

export default SalesSection;
